# 노무비 분석 및 적정성 판단 프로그램 - 사양서 (v0.1 Draft)

작성일: 2026-07-16
상태: 초안 (일부 항목 토론/확정 필요)

---

## 1. 개요

ERP에서 다운로드한 근로계약서 현황(xlsx)을 입력 받아, 개인정보를 마스킹 처리한 뒤
구글 스프레드시트에 저장하고, 이를 기반으로

- **사용자**: 본인/조직의 노무비가 적정한 수준인지 확인
- **어드민**: 전체 조직의 노임 추이(시계열 변화)까지 확인

할 수 있도록 하는 웹 애플리케이션이다.

---

## 2. 기술 스택 (확정)

| 영역 | 기술 | 비고 |
|---|---|---|
| 백엔드/런타임 | Google Apps Script (GAS) | 스프레드시트와 네이티브 연동 |
| 데이터 저장소 | Google Sheets | 별도 DB 없이 시트를 DB로 사용 |
| 프론트엔드 | HTML Service (GAS) + JavaScript | GAS가 서빙하는 웹앱 화면 |
| 차트 | Chart.js (JavaScript) | 노임 추이, 급여 분포 등 시각화 |
| 대시보드(일부) | Google Stitch (외주) | UI 디자인만 외주, 데이터 연동은 자체 개발 |
| 버전관리 | Git + GitHub | 로컬 git init 완료, 원격 저장소는 추후 연결 |
| 로컬 개발 도구 | clasp (Command Line Apps Script Projects) 권장 | 로컬에서 코드 작성 → GAS로 push, git으로 이력 관리 |

> 참고: 초기 요구사항에 언급된 "액션스크립트"는 Adobe Flash ActionScript가 아니라
> Google Apps Script를 의미하는 것으로 확정. "java 차트"도 Java가 아니라
> JavaScript 기반 차트 라이브러리(Chart.js)를 의미하는 것으로 확정.

---

## 3. 사용자 역할 및 권한

| 역할 | 로그인 화면 | 주요 기능 |
|---|---|---|
| 사용자 (User) | 별도 로그인 화면 | 노무비 적정성 확인만 가능 (본인/소속 범위) |
| 어드민 (Admin) | 별도 로그인 화면 | 적정성 확인 + 노임 추이 확인 + 데이터 전처리(업로드) |

- 로그인 방식: TBD — 자체 ID/PW (Sheet 기반 인증) vs Google 계정(Session.getActiveUser()) 중 선택 필요 → **토론 필요**
- 사용자별 조회 범위(본인만/부서 전체/전사) 도 권한 설계에 포함되어야 함 → **토론 필요**

---

## 4. 데이터 처리 흐름

```
[ERP 근로계약서 현황.xlsx]
        │  (어드민이 업로드)
        ▼
[전처리 단계 (GAS)]
  - 주민등록번호 뒷자리 마스킹 (예: 900101-1******)
  - 성명 마스킹 (예: 홍길동 → 홍OO, 요구사항 상 "최OO" 표기는 예시로 판단)
  - 사번(사원번호)을 고유 매칭 키로 사용
        ▼
[구글 스프레드시트 저장]
  - 원본 시트(비공개/접근제한) / 가공 시트(마스킹 완료, 서비스용) 분리 권장
        ▼
[웹앱 조회 화면]
  - 사용자 화면: 적정성 확인
  - 어드민 화면: 노임 추이 확인
```

- ERP xlsx 실제 컬럼 구조: **샘플 파일 전달 예정 → 도착 즉시 구조 분석 후 전처리 로직 확정**
- 마스킹은 "원본 → 가공" 1회성 배치 처리로 볼지, 매 업로드마다 누적/갱신할지 결정 필요 → **토론 필요**

---

## 5. 데이터 모델 (잠정안 — 샘플 파일 확인 후 확정)

### 5.1 시트: `EmployeeMaster` (직원 마스터, 마스킹 완료본)

| 컬럼 | 설명 | 비고 |
|---|---|---|
| 사번 | 고유 매칭 키 | PK |
| 성명(마스킹) | 예: 홍OO | 원본 성명은 미저장 또는 별도 비공개 시트 |
| 주민번호(마스킹) | 앞 6자리(생년월일)만 유지, 뒤 7자리는 마스킹 | 생년월일 그룹핑에 사용 |
| 생년월일 | 주민번호에서 추출 또는 별도 컬럼 | 나이대/연령그룹 계산용 |
| 직위/직군 | 공사/안전/품질/공무 등 | 직군별 검색 기준 |
| 입사일 | | |
| 재직상태 | 재직/퇴직 등 | |

### 5.2 시트: `WageRecords` (급여/계약 이력)

| 컬럼 | 설명 | 비고 |
|---|---|---|
| 사번 | FK | EmployeeMaster 참조 |
| 계약일자/기준일자 | 계약서 갱신 시점 | 노임 추이 시계열 축 |
| 급여(계약금액) | | |
| 업로드 배치 ID | 어떤 xlsx 업로드분인지 추적 | |

### 5.3 시트: `Users` (로그인용, 로그인 방식 확정 후 구체화)

---

## 6. 기능 명세

### 6.1 사용자 화면 — 노무비 적정성 확인
- 생년월일대(연령 그룹)별 평균급여 조회
- 급여 분포: 최저 / 중위(median) / 최대
- (논의) 직위(공사/안전/품질/공무)별 별도 검색
- (논의) 전체 나이대 검색 기능

### 6.2 어드민 화면 — 노임 추이
- 사번 기준 개인별 급여 변동 추적
- 전체 평균 상승률
- (논의) 그 외 추가 지표 — **토론 필요**

### 6.3 어드민 화면 — 전처리(데이터 업로드)
- ERP xlsx 업로드 → 마스킹 → 시트 저장
- 업로드 이력 관리(언제, 몇 건 처리됐는지)

---

## 7. 개인정보/보안 고려사항

- 주민등록번호 뒷자리, 성명 원본은 서비스용 시트에 저장하지 않음 (마스킹 후 데이터만 사용)
- 원본 xlsx 파일 자체의 보관/삭제 정책 필요 → **토론 필요**
- 구글 스프레드시트 공유 권한 최소화 (어드민만 원본 접근, 서비스 계정/웹앱을 통한 조회만 사용자에게 노출)
- 접근 로그(누가 언제 조회했는지) 필요 여부 → **토론 필요**

---

## 8. 미확정/토론 필요 항목 정리

| # | 항목 | 내용 |
|---|---|---|
| 1 | ERP xlsx 실제 컬럼 구조 | 샘플 파일 도착 후 확정 |
| 2 | 로그인 방식 | 자체 ID/PW vs Google 계정 연동 |
| 3 | 조회 권한 범위 | 사용자가 볼 수 있는 데이터 범위 (본인/부서/전사) |
| 4 | 직위별/나이대별 검색 기능 상세 | 요구사항 10번 |
| 5 | 노임추이 화면의 추가 기능 | 요구사항 11번 (사번추적, 상승률 외) |
| 6 | 원본 데이터 보관 정책 | 원본 xlsx, 마스킹 전 데이터 처리/삭제 규정 |
| 7 | GitHub 원격 저장소 연결 시점 | 현재는 로컬 git만 |
| 8 | Google Stitch 대시보드와 GAS 웹앱의 통합 방식 | 완전 별도 화면 vs iframe 임베드 등 |

---

## 9. 진행 순서 (제안)

1. ✅ 로컬 git 저장소 초기화
2. ⬜ ERP xlsx 샘플 파일 확인 → 데이터 모델 확정
3. ⬜ GAS 프로젝트 구조 세팅 (clasp 초기화, appsscript.json 등)
4. ⬜ 전처리 로직 구현 (마스킹, 사번 매칭)
5. ⬜ 사용자 화면 (적정성 확인) 구현
6. ⬜ 어드민 화면 (노임 추이) 구현
7. ⬜ 로그인/권한 처리
8. ⬜ Google Stitch 대시보드 연동
9. ⬜ GitHub 원격 저장소 연결 및 배포
